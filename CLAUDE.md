Act as a Senior Full-Stack Flutter Architect. I need you to design a robust, scalable, and production-ready project structure for a "Write Once, Run Anywhere" Flutter application targeting **Android, iOS, and Web**.

**Project Context:**
- **Goal:** Build a single codebase that adapts its UI/UX for Mobile (Touch) and Web (Mouse/Keyboard).
- **Architecture Pattern:** "Feature-First" combined with "Clean Architecture" (Separation of concerns: Presentation, Domain, Data layers).
- **Backend:** Firebase (Auth, Firestore, Storage, Functions).
- **Target Platforms:** Android, iOS, Web (PWA).

**Technological Stack & SDKs (STRICTLY use these):**
1.  **Core Framework:** Flutter (Latest Stable) & Dart 3 (Records, Patterns, Sealed Classes).
2.  **Multi-Platform & UI:**
    - `flutter_screenutil` or `layout` strategies: For responsive sizing.
    - `url_strategy`: To remove the hash (`#`) from Web URLs.
    - `google_fonts`: For consistent typography across platforms.
3.  **State Management:** `flutter_riverpod`, `riverpod_annotation`, `riverpod_generator`.
4.  **Navigation:** `go_router`: Essential for Web deep linking and browser history navigation.
5.  **Data & Models:** `freezed`, `freezed_annotation`, `json_serializable`.
6.  **Backend Integration:**
    - `firebase_core`, `firebase_auth`, `cloud_firestore`, `firebase_storage`.
    - **Crucial Configuration:** Use `DefaultFirebaseOptions` generated by the CLI.
7.  **Utilities:** `fpdart` (Functional error handling), `logger` (for debugging).

**Structure Requirements:**
1.  **Folder Hierarchy (ASCII Tree):**
    - **`lib/src/common_widgets/`**: Reusable UI components.
    - **`lib/src/utils/`**: Breakpoints (Mobile < 600px, Tablet < 1200px, Desktop > 1200px).
    - **`lib/src/features/`**:
        - Each feature (e.g., `auth`) must implement Clean Architecture (`data`, `domain`, `presentation`).
        - **Responsive UI:** Inside `presentation`, show how to separate `MyScreenMobile` and `MyScreenWeb`.
2.  **Configuration Strategy (IMPORTANT):**
    - In your instructions, explicitly state that the user **MUST run `flutterfire configure`** to generate the `firebase_options.dart` file.
    - Explain that this single file will handle API keys for Android, iOS, and Web automatically.
    - Show how to use `DefaultFirebaseOptions.currentPlatform` in `main.dart`.
3.  **Web Specifics:**
    - Include `url_strategy` setup in `main.dart` to ensure clean URLs (no `#`).

**Deliverables:**
1.  **Project Structure Tree:** A detailed ASCII representation.
2.  **`pubspec.yaml` Dependencies:** Complete list including web-specific plugins.
3.  **Key Code Snippets:**
    - **`main.dart`**: Complete entry point showing:
        - `WidgetsFlutterBinding.ensureInitialized()`
        - `setPathUrlStrategy()` (for Web)
        - `Firebase.initializeApp(options: DefaultFirebaseOptions.currentPlatform)`
    - **`ResponsiveScaffold`**: A generic widget implementation for handling Mobile vs Web layouts.